CreateXml         //创建Xml文件，并定义根节点及其子节点ID&Password
ReadXmlData       //读取Xml指定Key数据
SetXmlData        //向Xml中设置数据 (id, password)
{
创建key节点(id) //存在则无需创建，直接覆盖
该key节点append到ID & Password节点中
往节点中赋值(password)
SaveXmlDocument
}
ReBuildXml        //初始化Xml（运用File.Delete）


结构：
<Config>
  <ID & Password>
    <Administrator> Romantic </Administrator>
    <ID1> Password1 </ID1>
    <ID2> Password2 </ID2>
    <ID3> Password3 </ID3>
  </ID & Password>
</Config>